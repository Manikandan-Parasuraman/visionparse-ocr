version: "3.9"

services:
  backend:
    build: ./backend
    container_name: ocr-backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: ocr-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend